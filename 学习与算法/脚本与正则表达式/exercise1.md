
**第 1 题｜抽数字** 测试文本：

```
苹果 3 个，香蕉 12 根，橘子 100 斤
```

任务：匹配所有数字。 应匹配：`3`、`12`、`100`（共 3 个）

---

**第 2 题｜带单位** 测试文本：

```
包裹 25kg，信封 3kg，箱子 100kg
```

任务：只匹配「数字 + kg」整体。 应匹配：`25kg`、`3kg`、`100kg`

---

**第 3 题｜小数也要** 测试文本：

```
身高 1.75，体重 60，体温 36.5，鞋码 42
```

任务：整数和小数都要匹配到。 应匹配：`1.75`、`60`、`36.5`、`42`

---

**第 4 题｜字母数字组合** 测试文本：

```
订单号 A100，B23，用户 user_01，密码 x9
```

任务：匹配「字母开头，后面跟字母/数字/下划线」的整串。 应匹配：`A100`、`B23`、`user_01`、`x9`

---

**第 5 题｜锚点** 测试文本（注意这里有多行，把 flag 保持 `gm`）：

```
error: 磁盘满了error
warning: 内存偏高
error: 连接超时
```

任务：只匹配以 `error` **开头**的行里的 `error`。 应匹配：两个 `error`（开头的），不匹配行中间出现的话

---

**第 6 题｜或** 测试文本：

```
cat dog cat fish dog bird
```

任务：匹配 `cat` 或 `dog`。 应匹配：`cat`、`dog`、`cat`、`dog`（共 4 个）

---

**第 7 题｜简易日期** 测试文本：

```
2026-07-11 交作业，2025-1-3 是错误格式，2024-12-25 放假
```

任务：只匹配 `YYYY-MM-DD` 这种**四位-两位-两位**的标准格式。 应匹配：`2026-07-11`、`2024-12-25`（中间那个位数不够，不该匹配）

---

敲完对答案：

> [!success]- 参考答案（先自己试再看）
> 
> 1. `\d+`
> 2. `\d+kg`
> 3. `\d+(?:\.\d+)?` —— `?:` 是非捕获组，`?` 表示小数部分可有可无
> 4. `[A-Za-z]\w*` —— 字母开头，`\w*` 跟零个或多个 word char
> 5. `^error` —— `^` 配合多行 flag `m` 表示每行开头
> 6. `cat|dog`
> 7. `\d{4}-\d{2}-\d{2}` —— `{4}`/`{2}` 是精确次数

从第 1 题往下敲，卡在哪题告诉我题号，我给你拆。全过了的话我再上一组带 `?`懒惰匹配和捕获组提取的进阶题。

`cd /tmp && cat > correct.py << 'EOF' def cn_to_int(text): digits = {"零":0,"一":1,"二":2,"三":3,"四":4,"五":5, "六":6,"七":7,"八":8,"九":9} units = {"十":10,"百":100,"千":1000} total = 0 # 最终结果(累计已结算的"万/亿"节) section = 0 # 当前"万"节内的累计值 cur = 0 # 刚读到的、还没结算的个位数字 for ch in text: if ch in digits: cur = digits[ch] # 记住这个数字,等后面的单位来乘 elif ch in units: if cur == 0: cur = 1 # 处理"十五"这种十前无数字 → 当作一十 section += cur * units[ch] # 关键:乘上位权再加进 section cur = 0 elif ch == "万": section = (section + cur) * 10000 total += section section = cur = 0 elif ch == "亿": section = (section + cur) * 10**8 total += section section = cur = 0 return total + section + cur for t in ["三百二十一", "三百", "二十", "十五", "一千零五", "三万二千一百"]: print(f"{t:<8} -> {cn_to_int(t)}") EOF python3 correct.py`

Output

`三百二十一 -> 321 三百 -> 300 二十 -> 20 十五 -> 15 一千零五 -> 1005 三万二千一百 -> 32100`