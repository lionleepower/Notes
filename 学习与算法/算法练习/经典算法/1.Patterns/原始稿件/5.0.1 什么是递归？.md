#algorithm/binary-tree #algorithm/dfs #status/review

> [!info] 原始稿件导航
> 整理版：[[5.1 最大深度、最小深度与路径总和]] · 总览：[[0.0 Patterns 算法知识图谱]]

![[Pasted image 20260708122008.png]]

![[Pasted image 20260708122048.png]]
![[Pasted image 20260708122109.png]]
![[Pasted image 20260708122123.png]]
![[Pasted image 20260708122348.png]]从原问题出发，把问题不断分成无数个更小的子问题，这个就是递归中的（递）！
![[Pasted image 20260708122622.png]]
![[Pasted image 20260708123354.png]]

做法一：
```python
# Definition for a binary tree node.

# class TreeNode:

#     def __init__(self, val=0, left=None, right=None):

#         self.val = val

#         self.left = left

#         self.right = right

class Solution:

    def maxDepth(self, root: Optional[TreeNode]) -> int:

        if root is None:

            return 0

        l_depth=self.maxDepth(root.left)

        r_depth=self.maxDepth(root.right)

        return max(l_depth, r_depth)+1
```


做法二：
使用全局变量

```python
# Definition for a binary tree node.

# class TreeNode:

#     def __init__(self, val=0, left=None, right=None):

#         self.val = val

#         self.left = left

#         self.right = right

class Solution:

    def maxDepth(self, root: Optional[TreeNode]) -> int:

        ans=0

        def f(node,cnt):

            if node is None:

                return

            cnt+=1

            nonlocal ans

            ans=max(ans,cnt)

            f(node.left,cnt)

            f(node.right,cnt)

        f(root,0)

        return ans
```
