

![[Pasted image 20260707171344.png]]
![[Pasted image 20260707171447.png]]
根据快指针指向的位置来判断慢指针是否为中间节点，对于奇数长度，慢指针一定在中间节点，对于偶数长度，看看快指针是否指向空，所以就看快指针为空，或是快指针的next为空
![[Pasted image 20260707171653.png]]

![[Pasted image 20260707171235.png]]
```
# Definition for singly-linked list.

# class ListNode(object):

#     def __init__(self, val=0, next=None):

#         self.val = val

#         self.next = next

class Solution(object):

    def middleNode(self, head):

        """

        :type head: Optional[ListNode]

        :rtype: Optional[ListNode]

        """

        slow=head

        fast=head

        while fast and fast.next:

            slow=slow.next

            fast=fast.next.next

        return slow
```


![[Pasted image 20260707172402.png]]

```
# Definition for singly-linked list.

# class ListNode(object):

#     def __init__(self, x):

#         self.val = x

#         self.next = None

  

class Solution(object):

    def hasCycle(self, head):

        """

        :type head: ListNode

        :rtype: bool

        """

  

        slow=head

        fast=head

        while fast and fast.next:

            slow=slow.next

            fast=fast.next.next

            if fast is slow:

                return True

        return False
```
![[Pasted image 20260707172712.png]]
![[Pasted image 20260707172810.png]]


这里我没太理解，为什么一定会在入口处相遇，head到入口的距离为什么是环长的倍数
我懂了，是head最后一定会与slow point相遇，是head走了c步之后，到入口的距离是环长的倍数
且快慢指针相遇时，慢指针的距离必定小于环长，我们可以使用极端换情况来假设，就假设到达环长时，快指针只比慢指针快一个位置，即使这样，它们相遇也会只要环长-1个位置，所以慢指针的位置小于环长（我这解释的是对的吗）
![[Pasted image 20260707172902.png]]
![[Pasted image 20260707173508.png]]
![[Pasted image 20260707174926.png]]

```
# Definition for singly-linked list.

# class ListNode(object):

#     def __init__(self, x):

#         self.val = x

#         self.next = None

  

class Solution(object):

    def detectCycle(self, head):

        """

        :type head: ListNode

        :rtype: ListNode

        """

        slow=head

        fast=head

        while fast and fast.next:

            slow=slow.next

            fast=fast.next.next

            if fast is slow:

                while slow is not head:

                    slow = slow.next

                    head=head.next

                return slow

        return None
```

![[Pasted image 20260707175107.png]]
![[Pasted image 20260707175244.png]]
![[Pasted image 20260707175350.png]]
![[Pasted image 20260707180533.png]]

```
# Definition for singly-linked list.

# class ListNode:

#     def __init__(self, val=0, next=None):

#         self.val = val

#         self.next = next

class Solution:

    def middleNode(self,head):

        slow = head

        fast = head

        while fast and fast.next:

            slow=slow.next

            fast=fast.next.next

        return slow

  

    def reverseList(self,head):

        pre=None

        cur=head

        while cur:

            nxt=cur.next

            cur.next=pre

            pre=cur

            cur=nxt

        return pre

  

    def reorderList(self, head: Optional[ListNode]) -> None:

        """

        Do not return anything, modify head in-place instead.

        """

        mid = self.middleNode(head)

        head2=self.reverseList(mid)

        while head2.next:

            nxt = head.next

            nxt2=head2.next

            head.next=head2

            head2.next=nxt

            head=nxt

            head2=nxt2
```