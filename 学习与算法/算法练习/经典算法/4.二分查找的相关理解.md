1. 一定要明确我们需要算的结果，通常以结果为导向来设计二分查找
 
1. 学会提前剪枝， 尝试边算边判断
2. 设计算法可以提前写出input 和 output，避免混淆
3. 注意用zip来代替compustion[i][j]的使用￼

![Exported image](Exported%20image%2020260514133512-0.png)

class Solution(object):￼ def maxNumberOfAlloys(self, n, k, budget, composition, stock, cost):￼ """￼ :type n: int￼ :type k: int￼ :type budget: int￼ :type composition: List[List[int]]￼ :type stock: List[int]￼ :type cost: List[int]￼ :rtype: int￼ """￼ ans = 0￼ mx = min(stock) + budget￼ for comp in composition:￼ def check(num):￼ money = 0￼ for s, base, c in zip(stock, comp, cost):￼ if s \< base * num:￼ money += (base * num - s) * c￼ if money \> budget:￼ return False￼ return True￼ ￼ left, right = ans, mx + 1￼ while left + 1 \< right:￼ mid = (left + right) // 2￼ if check(mid):￼ left = mid￼ else:￼ right = mid￼ ans = left￼ return ans
 \> 来自 \<[https://leetcode.cn/problems/maximum-number-of-alloys/](https://leetcode.cn/problems/maximum-number-of-alloys/)\>