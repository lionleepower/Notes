# Python <主题>：<一句话说明>

用 1～2 句话说明这个语法、数据结构或语言特性解决什么问题，以及算法题中什么时候需要它。

---

## 思路

先说明为什么需要这个知识点，再解释它的核心机制。重点回答：不用它会有什么问题，它为什么适合当前场景。

> [!tip]- 核心思路
> 用最短的语言写出核心结论。
>
> ```python
> # 放置最常用、最容易记忆的核心写法
> result = ...
> ```
>
> 补充一句判断依据或不变量。

---

## 1. 导入与创建

说明是否需要导入，以及常见的初始化方式。

> [!success]- 基础语法
> ```python
> # 必要导入
> # from module import name
>
> empty = ...
> initialized = ...
> ```

---

## 2. 核心操作

| 操作 | 作用 | 时间复杂度或注意事项 |
| --- | --- | --- |
| `operation_one()` | 说明作用 | $O(1)$ 或具体限制 |
| `operation_two()` | 说明作用 | $O(n)$ 或具体限制 |

> [!example]- 最小示例
> ```python
> # 示例应当可以独立理解和运行
> value = ...
> print(value)
> ```

---

## 3. 关键行为与边界

说明最容易被误解的语义、边界条件或不同写法之间的差异。

> [!tip]- 为什么这样做
> 解释结果背后的原因，不只记录表面现象。

> [!warning]- 边界条件
> - 空输入如何处理。
> - 参数的合法范围。
> - 重复元素、负数、`None` 或极端规模如何处理。

---

## 4. 实例一：<最常见使用场景>

先用一句话说明输入、目标以及为什么选择当前语法或数据结构。

> [!success]- 完整实现
> ```python
> def example_one(data):
>     # 保留能解释核心思路的完整代码
>     return data
>
>
> print(example_one([]))
> ```

说明主流程、关键变量和最容易写错的位置。

---

## 5. 实例二：<算法题中的典型应用>

选择一个能体现该知识点价值的实例，不要只更换输入数据重复上一节。

> [!success]- 完整实现
> ```python
> def example_two(data):
>     result = []
>     for item in data:
>         result.append(item)
>     return result
> ```

> [!summary]- 当前实例复杂度
> - 时间复杂度：$O(n)$，说明原因。
> - 空间复杂度：$O(n)$，区分额外空间与返回结果空间。

---

## 6. 与相近写法怎么选

| 需求 | 推荐写法 | 原因 |
| --- | --- | --- |
| 场景一 | 写法 A | 说明优势 |
| 场景二 | 写法 B | 说明优势 |

只保留真正会影响选择的差异，不为了完整而罗列无关方法。

---

## 7. 常见易错点

> [!warning]- 易错点
> - 错误一：说明错误为什么发生，以及正确写法。
> - 错误二：说明边界条件。
> - 错误三：说明返回值、更新顺序或对象共享问题。

---

## 复杂度

> [!summary]- 复杂度
> - 核心操作：时间复杂度为 $O(\ldots)$，因为……
> - 完整流程：时间复杂度为 $O(\ldots)$。
> - 额外空间：$O(\ldots)$，说明空间来自哪里。

---

## 一句话总结

用一句话同时写出这个知识点的本质、触发信号和最关键写法。

---

## 模板归纳

看到以下信号时，优先想到当前知识点：

- 触发信号一
- 触发信号二
- 触发信号三

```python
# 可直接复用的最小模板
def solve(data):
    result = []
    for item in data:
        result.append(item)
    return result
```

如果存在真实的前置、同类或进阶笔记，在最后补充 Wiki 链接；没有真实目标时不添加占位链接。
