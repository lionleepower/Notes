![Exported image](Exported%20image%2020260514133516-0.png)  
![Exported image](Exported%20image%2020260514133518-1.png)     

```
class Solution(object):
    def findPeakElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        left = -1
        right = len(nums) - 1
        while left + 1 \< right:
            mid = (left + right) //2
            if nums[mid] \> nums[mid+1]:
                right = mid
            else:
                left = mid
        return right
```
      
![Exported image](Exported%20image%2020260514133519-2.png)  
![Exported image](Exported%20image%2020260514133520-3.png)  

```
class Solution(object):
    def findMin(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        left = -1
        right = len(nums) - 1
        while left + 1 \< right:
            mid = (left + right) //2
            if nums[mid] \< nums[-1]:
                right = mid
            else:
                left =mid
        return nums[right]
```
      
![Exported image](Exported%20image%2020260514133521-4.png)

```
        
class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        def is_blue(i):
            end =nums[-1]
            if nums[i] \> end:
                return target \> end and nums[i] \>=target
            else:
                return target \> end or nums[i] \>=target
        left = -1
        right = len(nums)
        while left +1 \< right:
            mid = (left + right) //2
            if is_blue(mid):
                right = mid
            else:
                left = mid
        if right == len(nums) or nums[right] != target:
            return -1
        return right 
```