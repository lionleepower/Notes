|优先级|主题|必须能答的考点|项目里的追问入口（hook）|开始时间|
|---|---|---|---|---|
|**P0**|SQL / 数据库|B+ 树索引、复合索引与最左前缀、覆盖索引、`GROUP BY` 取整行为什么错 + 窗口函数 `ROW_NUMBER() OVER (PARTITION BY)`、事务 ACID、隔离级别、批量插入为什么要包事务|`db.py` 的 3 个复合索引为什么这么建；D1 的 Q4 就是这道题；importer 的事务处理|**今天**|
|**P0**|并行计算概念|进程 vs 线程 vs 协程、**Amdahl 定律**、强/弱扩展、并行效率为何可 >100%（cache 效应）、MPI ranks=进程 / OpenMP threads=线程|`metrics.py` 整个模块；Q1-Q3；**双赛道通吃**——Agent 岗面试官看到 HPC 项目必问一句，芯片岗更是主菜|**今天**|
|**P0**|Python 核心|GIL、生成器/迭代器、上下文管理器（`with` 背后的协议）、dict 底层、异常处理规范|`db.py` 的 `with connect(...)`、importer 逐行流式读 CSV 为什么不整个 load|本周|
|**P1**|LLM 应用八股|function calling 完整机制（schema→选择→参数抽取）、temperature/top-p、token 与上下文窗口、幻觉与 grounding、prompt injection 防御、**怎么评测 Agent**|W2 的 `llm_router.py` 和评测集；"为什么 temperature=0""LLM 挂了怎么办"（你有降级答案）|W2 开始前（~7/14）|
|**P1**|HTTP / 网络|REST 语义、常见状态码、幂等性、超时与重试策略、TCP 三次握手、HTTPS/TLS 概览|调 LLM API 的重试降级逻辑；为 W3 FastAPI 铺路|W2|
|**P2**|后端工程|async/await 与 ASGI vs WSGI、FastAPI 参数校验与统一异常、限流/幂等概念|W3 的 `api.py`，每个接口设计都是活例子|W3 开始前（~7/21）|
|**P2**|可观测性|结构化日志 vs print、TraceID 如何串联一次调用链、日志分级|W3 的 `tracing.py`|W3|
|**P3**|Redis / 缓存|只到概念级：TTL、穿透/击穿/雪崩、缓存一致性思路|简历已砍 Redis——目标是被问"为什么不加缓存"时能从容答"评估过，QPS 不需要，加了反而要处理一致性"，这比硬实现更加分|有空再看|
|**P3**|Docker / Linux|镜像 vs 容器、常用命令、进程查看排查|技能栏一句话水平即可|有空再看|
|**加试包**|芯片生态岗专项|MPI 通信原语（点对点/集合通信）、OpenMP 调度策略、cache 层级与 NUMA、`-O2/-O3` 编译优化、roofline 模型概念|复盘 `petsc-benchmark-suite` 的真实实验设计|**约到面试后集训 2-3 天**，平时不投入|