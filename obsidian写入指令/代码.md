请把我提供的算法题内容整理成适合直接复制到 Obsidian 的 Markdown 笔记。

要求：

1. 中文解释为主，保留必要英文术语，例如 `Two Pointers`、`Sliding Window`、`Hash Map`、`DFS`、`DP`。
    
2. 内容保持简洁、高效，不要写成长篇教程，只保留理解和复习最关键的信息。
    
3. 按照下面结构整理：
    

# 题目名称

## 1. 题目要解决什么问题？

用 2-4 句话解释题意，说明输入、输出和目标。

## 2. 核心思路

说明这道题属于什么题型，以及为什么可以这样做。

例如：

- 双指针：为什么移动左指针或右指针
    
- 滑动窗口：窗口维护什么，什么时候扩张，什么时候收缩
    
- 哈希表：为什么需要记录映射关系
    
- 动态规划：状态是什么，转移方程是什么
    

## 3. 关键公式 / 判断条件

如果有公式，用 Obsidian 兼容格式：

行内公式使用 `$...$`

独立公式使用：

```markdown
$$
公式
$$
```

不要使用 `\(...\)` 或 `\[...\]`。

如果公式里有下划线变量，例如 `pre_max`，写成：

```markdown
$$
\mathrm{pre\_max}[i]
$$
```

不要写成：

```markdown
$$
\text{pre_max}[i]
$$
```

## 4. 代码模板

给出干净、可直接复习的 Python 代码。

要求：

- 变量名清晰
    
- 保留关键注释
    
- 不要在代码块后加入 `id="..."`
    
- 不要写过多废话注释
    

## 5. 复杂度分析

写出：

```markdown
时间复杂度：

$$
O(...)
$$

空间复杂度：

$$
O(...)
$$
```

并用一句话解释原因。

## 6. 核心结论

用 3-6 条 bullet points 总结这道题最重要的结论。

## 7. 常见误区

列出 2-4 个最容易写错或理解错的点。

整体风格要求：

- 不要太长
    
- 不要堆概念
    
- 不要写鸡汤
    
- 不要省略关键推导
    
- 抽象概念必须配一个小例子
    
- 输出必须是完整 Markdown，方便我直接复制到 Obsidian