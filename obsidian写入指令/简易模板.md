请把下面内容整理成适合直接复制到 Obsidian 的 Markdown 笔记。

要求：

- 中文解释为主，保留关键英文术语
    
- 从基础概念开始，以问题为导向，不省略关键推导
    
- 抽象概念需要配具体例子
    
- 数学公式使用 Obsidian 兼容格式：
    
    - 行内公式 `$...$`
        
    - 独立公式 `$$...$$`
        
- 不要使用 `\(...\)`、`\[...\]`
    
- 不要在代码块后加入 `id="..."`
    
- 代码和命令使用 Markdown 代码块
- 在 Obsidian 中，独立公式应当直接使用：

```
$$公式$$
```

不要写成：

```
## $$公式$$
```

也不要在公式内部的每一行前面加 `##`

```
公式里遇到 `MAX_CORES` 这种带下划线的变量
写成：```markdown$$\mathrm{MAX\_CORES}$$
```

不要写成：

```
$$\text{MAX_CORES}$$
```
    
- 最后增加术语表、核心结论、常见误区、自测问题
    

原始内容：

[粘贴内容]

我最不理解的地方：

[填写问题]